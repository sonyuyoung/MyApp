<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" >

    </style>
    <style name="myToolBarCss">
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:background">@color/design_default_color_primary</item>
        <item name="android:fontFamily">@font/nexonbold</item>
    </style>
    <style name="myButton">
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:background">@color/design_default_color_on_secondary</item>
        <item name="android:fontFamily">@font/nexonmed</item>
    </style>
</resources>